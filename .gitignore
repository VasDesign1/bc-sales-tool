# Local-only files (working notes, dropped artefacts) — never push to the public repo
_docx/
Prompt.docx
*.docx
*.xlsx

# Local credential drops — NEVER commit (NAV web-service key, etc.)
nav-creds.local.txt
*.local.txt
refresh_token.txt
